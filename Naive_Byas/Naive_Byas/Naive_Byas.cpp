#include <iostream>
#include "fstream"

using namespace std;

typedef int T;

struct note
{
	T age;
	T salary;
	bool is_stud;
	T cred_reit;
	bool sale;
}

bool processing(note* list, int ind, T age, T salary, bool is_stud, T cred_rait)
{
	double p_y[5];
	double p_n[5];

}

int main()
{
	note list[5];
	list[1].age=1;
	cout<<"Hello\n";
	system("pause");
	return 0;
}